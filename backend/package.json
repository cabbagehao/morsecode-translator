{
  "name": "morse-coder-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for uploading debug files to R2",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "react-markdown": "^10.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
